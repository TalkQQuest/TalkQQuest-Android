*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# IDE 개인 설정 전체 제외 (팀 충돌 방지)
/.idea/

# 빌드 산출물 전량 제외.
# /build 하나만 두면 맥에서 폴더가 중복 복사될 때 생기는 "build 2", "build 4" 같은
# 이름이 걸러지지 않아 생성 파일이 그대로 커밋된다(실제로 72개가 올라온 적 있음).
build/
build [0-9]*/
**/build/
**/build [0-9]*/
.kotlin/
