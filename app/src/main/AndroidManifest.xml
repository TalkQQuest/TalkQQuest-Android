<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ 알림 표시 런타임 권한 (FCM 푸시) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 프로필 사진 선택을 위한 MediaStore 읽기 권한. Android 14+에서는 선택한 사진만
         허용된 경우에도 READ_MEDIA_VISUAL_USER_SELECTED로 목록을 안전하게 조회한다. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- TODO: 카카오/네이버 SDK 키 확정 후 추가
    카카오 예시:
    <meta-data android:name="com.kakao.sdk.AppKey" android:value="@string/kakao_app_key" />
    <activity android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity" ...>
        <intent-filter> <data android:scheme="kakao{NATIVE_APP_KEY}" /> </intent-filter>
    </activity>
    네이버 예시:
    <meta-data android:name="com.naver.sdk.clientId" android:value="@string/naver_client_id" />
    <meta-data android:name="com.naver.sdk.clientSecret" android:value="@string/naver_client_secret" />
    <meta-data android:name="com.naver.sdk.clientName" android:value="@string/naver_client_name" />
    -->

    <application
        android:name=".TalkQQuestApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalkQQuest">
        <!-- adjustResize 명시: 미지정이면 시스템이 adjustPan(화면 통째로 밀기)을 골라
             키보드 시 헤더가 밀려나가고 imePadding과 이중 보정돼 입력창 위 빈 공간이 생김 -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:scheme="kakao${KAKAO_NATIVE_APP_KEY}" />
            </intent-filter>
        </activity>
        <!-- FCM 수신 서비스 (토큰 갱신·메시지 수신) -->
        <service
            android:name=".core.push.TqMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- 백그라운드 알림 메시지가 쓸 기본 채널 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- portrait 고정: 디자인이 393×852 세로 기준이고 FitDesign도 세로 비율로 축소율을 잡는다.
             가로에서는 축소율 계산이 무너져 레이아웃이 늘어난다. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TalkQQuest.Starting"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
